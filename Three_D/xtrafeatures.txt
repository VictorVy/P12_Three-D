improved cursor movement (no wonky jittering/shaking)

vertical movement (shift/space to move up/down)

third-person view (mouse scroll wheel to zoom in/out) (with improved camera movement!)

snow cloud (randomly produces randomly-moving snowflakes under itself) (one follows the player around)

mode framework (try out all the modes!) (press 'p' to pause/unpause)

"interesting" menus (text "looks" at mouse) ("floating" buttons)

object heirarchy (eg. Button extends RPrism, FloorBlock extends Block) (abstract Bullet class)

enemies aim more accurately (exponential mapping function) (also range cutoff)